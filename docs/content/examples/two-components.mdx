---
title: Two connected components
description: Connect named pins on two placed chip instances.
---

import { twoComponentsProgram } from "../../examples/sources";

# Two connected components

Aliases keep a connection readable even when the physical pin numbers differ between components.

<BreadboardExample
  label="Two connected chip instances"
  tip="Connect A.IN to B.OUT instead, or add ground wires for both chips."
  source={twoComponentsProgram}
/>
