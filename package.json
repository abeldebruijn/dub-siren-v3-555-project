{
  "name": "dub-siren-v3",
  "private": true,
  "packageManager": "pnpm@10.19.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check:release": "pnpm test && pnpm build"
  }
}
